import 'package:flutter/material.dart';

class GirlsListScreen extends StatelessWidget {
  const GirlsListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(body: Container(child: Text("Girls List Details"),),));
  }
}
