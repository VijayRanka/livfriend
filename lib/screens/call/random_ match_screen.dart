import 'package:flutter/material.dart';

class RandomMatchScreen extends StatelessWidget {
  const RandomMatchScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(body: Container(child: Text("Message Details"),),));
  }
}
