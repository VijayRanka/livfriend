import 'package:flutter/material.dart';

class TalktimeWidget extends StatelessWidget {
  const TalktimeWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Talktime widget"));
  }
}
