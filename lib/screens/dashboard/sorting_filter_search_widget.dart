import 'package:flutter/material.dart';

class SortingFilterSearchWidget extends StatelessWidget {
  const SortingFilterSearchWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(body: Container(child: Text("Message Details"),),));
  }
}
